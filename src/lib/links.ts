/**
 * Where this app's source lives. Shown on the setup screen and in the header, because a page that
 * asks for a GitHub token should make it easy to go read what it does with it.
 */
export const SOURCE_URL = 'https://github.com/rubensworks/renovate-overview/';

/**
 * The form for creating a fine-grained personal access token.
 */
export const TOKEN_URL = 'https://github.com/settings/personal-access-tokens/new';
